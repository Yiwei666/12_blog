# 电脑组装配置（二）


# 1. windows系统安装

### 1. 安装前准备

在安装 Windows 11 专业版之前，需要准备：

| 项目 | 说明 |
|---|---|
| U 盘 | 建议 16GB 或以上，制作安装盘时会被清空 |
| 旧电脑/笔记本 | 用于下载并制作 Windows 11 安装 U 盘 |
| 新电脑 | 已完成硬件组装，并确认可以点亮进入 BIOS |
| 键盘鼠标 | 建议插在主板后置 USB 接口 |
| 显示器 | 显示器 DP/HDMI 线应连接到独立显卡接口，而不是主板接口 |

新电脑第一次点亮时，如果能进入 BIOS，并能识别 CPU、内存、硬盘，说明核心硬件基本正常。


### 2. 制作 Windows 11 安装 U 盘

> 首先在旧电脑上打开微软 Windows 11 [官方下载页面](https://www.microsoft.com/en-us/software-download/windows11)，下载 `Media Creation Tool`，也就是 Windows 11 安装媒体创建工具。

> 注意不要下载或复制`Windows 11 安装助手`。安装助手主要用于已有 Windows 系统的在线升级，而新电脑全新安装系统应使用 `Media Creation Tool` 制作启动 U 盘。

操作流程如下：

```
1. 插入 U 盘
2. 运行 Media Creation Tool
3. 选择“为另一台电脑创建安装介质”
4. 语言选择：中文（简体）
5. 版本选择：Windows 11
6. 体系结构选择：64 位
7. 介质选择：U 盘
8. 等待工具自动下载并制作完成
```

制作工具界面中只显示 “Windows 11”，不会提前显示“Windows 11 专业版”。


### 3. 从 U 盘启动新电脑

1. 制作好安装 U 盘后，将 U 盘插入新电脑主板后置 USB 接口。

2. 开机后连续按：`F11`

3. 进入 MSI 主板的启动菜单，然后选择类似下面的启动项：`EFI: U盘名称`。建议选择带有 UEFI 字样的 U 盘启动项。


### 4. 开始安装 Windows 11

1. 进入 Windows 安装界面后，依次选择：

```
语言：中文（简体）
时间和货币格式：中国
键盘输入法：微软拼音
```

如果系统询问产品密钥，可以选择：`我没有产品密钥`

2. 之后在版本列表中选择：

```
Windows 11 Pro
或
Windows 11 专业版
```

注意这里不要选家庭版。可以先不激活，进入系统后再输入专业版密钥激活。


### 5. 选择安装方式和硬盘

1. 安装类型选择：`自定义：仅安装 Windows（高级）`

不要选择“升级”，因为这是新电脑全新安装系统。


2. 在硬盘选择界面中，如果是新硬盘，会看到类似：

```
驱动器 0 未分配空间
容量约 1.86TB
```

本次使用的是 2TB NVMe SSD，Windows 中显示约 1.81TB 或 1.86TB 属于正常现象。

最简单稳妥的做法是：

```
选择“驱动器 0 未分配空间”
点击“下一步”
```

Windows 会自动创建 EFI 系统分区、MSR 保留分区、C 盘主分区和恢复分区。

如果后续想分成 C 盘和 D 盘，不一定要在安装时手动分区，可以先让 Windows 自动安装，进入系统后再用“磁盘管理”压缩 C 盘并新建 D 盘。


### 6. 等待系统自动安装

接下来 Windows 会自动完成：

```
复制文件
安装功能
安装更新
自动重启
```

电脑重启后，不要反复按 F11，否则可能再次进入 U 盘安装界面。

如果重启后又进入安装程序，可以拔掉 U 盘，或者在 BIOS/启动菜单中选择硬盘启动。


### 7. 首次进入 Windows 设置

安装完成后，会进入 Windows 11 首次设置界面，需要设置地区、键盘、账户、隐私选项等。

如果进入联网界面时提示：

```
以太网 未连接
下一步按钮不可用
```

说明系统当前没有联网，可能是网线未插，也可能是新主板网卡驱动暂时未识别。

推荐插网线进行连接。


### 8. 进入桌面后的基础设置

进入桌面后，由于 27 英寸 4K 显示器默认显示比例较小，需要先调整缩放。

路径：

```
右键桌面空白处
→ 显示设置
→ 缩放
```

建议设置为：

```
分辨率：3840 × 2160
缩放：150% 或 175%
```

对于 27 英寸 4K 显示器，通常推荐 150%；如果觉得文字仍然偏小，可以改为 175%。


### 9. 安装驱动和系统更新

系统安装完成后，建议按以下顺序处理：

```
1. 运行 Windows Update，安装系统更新
2. 安装 AMD 芯片组驱动
3. 安装 NVIDIA 显卡驱动
4. 安装 MSI 主板网卡、WiFi、蓝牙、声卡等驱动
5. 检查设备管理器是否有黄色感叹号
```

显卡驱动可以选择 NVIDIA 官方驱动。偏游戏可选 `Game Ready Driver`，偏科研建模、稳定性可选 `Studio Driver`。


### 10. 硬盘分区建议

如果系统安装完成后，C 盘显示约 1.81TB，可以在 Windows 中将其分成 C 盘和 D 盘。

操作路径：

```
右键开始菜单
→ 磁盘管理
→ 右键 C 盘
→ 压缩卷
→ 输入要分给 D 盘的空间
→ 在未分配空间上右键
→ 新建简单卷
→ 分配盘符 D
→ 格式化为 NTFS
```

如果软件较多，C 盘可以留 800GB–1TB；如果主要把数据放 D 盘，C 盘 500GB 也足够。


### 11. 安装完成后的检查

最后建议检查：

```
1. 任务管理器 → 性能
2. 设备管理器
3. 磁盘管理
4. 显示设置
5. Windows 激活状态
```

重点确认：

```
CPU 是否识别为 Ryzen 7 9700X
内存是否为 32GB
硬盘是否为 WD_BLACK SN850X 2TB
显卡是否为 RTX 5060 Ti
显示器是否运行在 3840 × 2160 分辨率
设备管理器是否无黄色感叹号
```



# 2. 驱动下载和更新

## 1. nvidia显卡驱动

- 驱动查找网址：https://www.nvidia.cn/geforce/drivers/
- `NVIDIA GeForce RTX 5060 Ti | Windows 11 > NVIDIA Studio` 驱动程序下载网址：https://www.nvidia.cn/geforce/drivers/details/271617/

<p align="center">
<img src="https://19640810.xyz/05_image/01_imageHost/20260601-214455.png" alt="Image Description" width="700">
</p>





# 3. 电脑压测和记录

## 1. 系统与硬件监控：`hwinfo`

> HWiNFO 是一款专业的系统信息与硬件监控工具，支持 Windows 和 DOS 平台，能够深度检测 CPU、GPU、主板、内存、存储等几乎所有硬件组件的详细参数。
> 
> 它提供实时传感器监控功能，可追踪温度、电压、风扇转速、功耗等数据，广泛用于硬件诊断、超频调试和系统稳定性测试。由于其数据准确、覆盖全面，HWiNFO 深受硬件爱好者、技术人员和游戏玩家的青睐。

官网下载：https://www.hwinfo.com/download/

<p align="center">
<img src="https://19640810.xyz/05_image/01_imageHost/20260531-211633.png" alt="Image Description" width="700">
</p>

注意：

- 强烈建议选择 Local (U.S.) 或者 FOSSHUB 节点下的 "Portable"（绿色便携版）。
- 便携版下载下来是一个压缩包，解压后直接双击就能运行，不需要安装到系统里，干净清爽，测试完不想用随时可以删掉。
- 双击 `HWiNFO64.exe` 后，会弹出一个小的启动窗口，勾选 "Sensors-only"（仅传感器），这个模式会直接打开包含温度、电压、频率和功耗的详细表格。
- 可以一边开着 HWiNFO64，一边运行压测软件（如 AIDA64 或 Cinebench），它会实时记录下最高温度和平均温度。




# 装机必备工具

# 1. 监测工具

## 1. iftop

1. 功能：监控实时网络流量，显示哪些连接正在占用带宽

2. 测试命令

```bash
iftop -i eth0
```

3. 示例输出

<p align="center">
<img src="https://19640810.xyz/05_image/01_imageHost/20250403-172815.png" alt="Image Description" width="1000">
</p>

- 在 iftop 界面底部，有几行统计信息：
  - `TX / RX`：当前发送/接收带宽。
  - `TOTAL`：总的上传、下载速率或数据量。

- 在 iftop 的默认显示中，最右边的三列数据分别代表不同时间窗口内的平均带宽使用情况，通常为：
  - 第一列：最近 2 秒 的平均数据传输速率
  - 第二列：最近 10 秒 的平均数据传输速率
  - 第三列：最近 40 秒 的平均数据传输速率





## 2. 目录树`tree`

如果希望以层级树的形式查看，并保留文件权限和属主信息，可以使用 tree 命令。

```sh
apt update && apt install tree -y

# 只查看当前目录下两层
alias ltree='tree -pugsh -L 2'
```



## 3. 时间同步工具chrony

chrony 是一种用于计算机系统的时间同步工具，用来通过网络（如 NTP）持续校准本机时间，使其保持准确。

```sh
# 判断是否已经安装了chrony
systemctl status chrony

# 安装 chrony
apt update
apt install -y chrony

# 查看当前系统时间与“标准时间”的偏差，以及同步质量
chronyc tracking
```

- `chronyc tracking`结果分析：

```sh
# 当前使用的时间服务器标识（即正在同步的上游时间源）
Reference ID    : 4559CF63 (ntp1.wiktel.com)

# 时间源层级（表示距离原子钟的“级别”，数值越小越接近原子钟）
Stratum         : 2

# 最近一次成功从时间服务器获取时间的时间点（以 UTC 表示）
Ref time (UTC)  : Tue Apr 21 09:52:25 2026

# 当前系统时间相对于标准时间的偏差
System time     : 0.000083163 seconds slow of NTP time

# 最近一次测量到的时间偏移量
Last offset     : -0.000000152 seconds

# 一段时间内的平均偏差（反映整体稳定性）
RMS offset      : 0.001456619 seconds

# 系统时钟本身的频率偏差（chrony 正在校正的快慢趋势）
Frequency       : 26.457 ppm slow

# 频率校正后剩余的误差
Residual freq   : -0.010 ppm

# 对频率估计的不确定度（误差范围）
Skew            : 0.745 ppm

# 本机到最终时间源的总网络延迟
Root delay      : 0.060231116 seconds

# 到根时间源的累计误差估计（时间不确定性）
Root dispersion : 0.001087781 seconds

# 两次时间同步之间的时间间隔
Update interval : 64.6 seconds

# 闰秒状态及当前是否处于正常同步状态
Leap status     : Normal
```









